machine:
  python:
    version: 3.6.1

dependencies:
  pre:
    - pip install wheel
    - pip install miyadaiku.themes.bootstrap4 miyadaiku.themes.fontawesome miyadaiku.themes.fullpagejs

test:
  override:
    - "true"

deployment:
  github:
    branch: master
    commands:
      - ./deploy_ghp.sh
